# Generated by IBM TransformationAdvisor
# Wed Nov 06 09:15:44 UTC 2019

apiVersion: v1
kind: Service
metadata:
  name: ibmexceptional
  labels:
    app: ibmexceptional
spec:
  ports:
    - port: 9080
  selector:
    app: ibmexceptional
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: ibmexceptional
  name: ibmexceptional
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: ibmexceptional
    spec:
      containers:
      - image: mycluster.icp:8500/default/ibmexceptional:latest
        name: ibmexceptional
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
